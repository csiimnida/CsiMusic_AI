# 개발일지 — 가사 AI 에이전트

---

## 1. 왜 만들었나

스포티파이의 실시간 가사 화면이 신기해서, **유튜브 영상을 주면 그 곡의 가사를 찾아
노래에 맞춰 띄워주는 것**을 만들어보기로 했다.

LRCLIB 에서 가사를 받아 붙이는 것 자체는 금방 됐는데, **내가 실제로 듣는 곡의
상당수에서 실패했다.** VTuber 커버, `歌ってみた` 영상, 동인곡 — 이런 영상은 제목이
`【歌ってみた】곡명 / covered by 가수명` 같은 형태라 정규식으로 곡명과 아티스트를 뽑기가 어렵다.

더 나쁜 건 **엉뚱한 가사가 뜨는 경우**였다. 제목 유사도로 매칭하니까 제목만 비슷한 다른 곡의
가사가 붙어 버린다. 사용자 입장에서는 "가사가 없다"보다 "가사가 틀렸다"가 훨씬 나쁘다.

그리고 일본 노래를 **따라 부르고 싶었다.** 가사가 있어도 일본어를 못 읽으면 소용이 없다.

이 세 문제는 전부 **문자열 처리로는 풀리지 않는다.** 그래서 AI 를 붙였다.

---

## 2. 개발 순서 — 단계적으로 넣었다

| 순서 | 커밋 | 내용 |
|---|---|---|
| 1 | `e526d68` | 가사 소스에 Unison 추가 (videoId 정확매칭 · 단어 단위 싱크) |
| 2 | `fe92414` | 가사 검색 입력을 유튜브 음악 메타로 · 일괄 찾기에 자막 싱크 연결 |
| 3 | `c4a697b` | **가사 검색에 AI 보조 추가 (제목 파싱 A · 가사 검증 B)** |
| 4 | `e410be2` | AI 보조의 지연·빈응답 리스크 차단 + **곡당 검증 예산** |
| 5 | `783900b` | **AI 가사 검증(B)을 기본 꺼짐으로 — A 부터 단계적 도입** |
| 6 | `3bb0768` | 음악 메타 AI 검증 + 사람 자막에서 가사 만들기 |
| 7 | `bb9b7ac` | **언어 가드가 커버 영상에서 올바른 원곡 가사를 거부하던 문제 수정** |
| 8 | `9f8c32c` | AI 검증(B) **기본 켜짐** + 관리자 화면에 AI UI |
| 9 | `9db0611` | 애매한 제목의 곡명·가수를 AI 로 가른다 (도구 A 재사용) |
| 10 | `7473187` → `701b5c2` | **설계 문서 작성 → 결정사항 반영** |
| 11 | `7977e75` | **일본어 가사에 한글 발음·번역을 붙인다 (도구 C)** |
| 12 | `1f4d3ae` | 가사가 없는 곡을 나무위키에서 가져온다 |
| 13 | `6a2b38e` | 나무위키 **출처 표기** |
| 14 | `91d9a1a` | 나무위키 검색어가 보컬 이름만 남던 것 수정 |

### 여기서 의도적으로 한 두 가지

**① 위험한 기능은 기본 꺼짐으로 넣었다 (5 → 8)**

도구 B(가사 검증)는 이 프로젝트에서 **유일하게 가사를 없앨 수 있는 기능**이다.
오거부하면 그 곡은 7일간 "가사 없음"으로 캐시된다.

그래서 처음엔 `AI_VERIFY_ENABLED=false` 로 넣고, 도구 A 만 켠 채로 한동안 썼다.
로그의 `AI 가사 거부:` 를 지켜보며 오거부가 없는 걸 확인한 뒤에야 기본 켜짐으로 승격했다.

**② 코드보다 설계 문서를 먼저 썼다 (10 → 11)**

도구 C 는 앞의 둘과 성격이 달라서(생성 작업 · 폴백 없음) 무작정 만들면 안 될 것 같았다.
[`docs/LYRICS_LAYERS_PLAN.md`](docs/LYRICS_LAYERS_PLAN.md) 627줄을 먼저 쓰고,
"일본어만 / 요청할 때만 / 항상 표시 / 판별은 유니코드로" 를 정한 뒤 구현했다.

결과적으로 **구현이 훨씬 빨랐다.** 청크 크기, 캐시 키, 검증 규칙을 코드 쓰기 전에 정해 뒀기 때문이다.

---

## 3. 트러블슈팅

실제로 겪은 것들이다. 대부분 코드에 주석으로 남겨 뒀다.

### 3.1 응답 본문이 비어서 온다

**증상**: API 는 200 을 주는데 `candidates[0].content.parts` 에 텍스트가 없다.

**원인**: **사고(thinking) 토큰이 `maxOutputTokens` 를 먼저 다 써 버린다.**
Gemini 의 사고 토큰은 출력 한도에서 나가는데, 상한을 2048 로 잡아 두니 사고가 그걸 다 먹고
본문을 쓸 자리가 없었다.

**찾는 데 오래 걸린 이유**: 처음엔 그냥 `null` 을 반환하고 끝냈다. 원인을 알 방법이 없었다.

**해결**:
```csharp
log.LogWarning("AI 응답에 본문이 없다 (finishReason={Reason}) — 사고 예산/안전필터를 확인하라",
    FinishReason(doc.RootElement) ?? "unknown");
```
`finishReason` 을 로그에 남기자 `MAX_TOKENS` 가 바로 보였다. 빈 본문의 원인은 대개
`MAX_TOKENS`(사고 토큰 초과) 아니면 `SAFETY`(안전 필터)인데, **둘은 대응이 전혀 다르다.**

그 뒤 사고를 최소치로 누르고, 생성 작업(도구 C)만 상한을 4096 / 8192 로 올렸다.

> **교훈**: 조용히 실패하면 원인을 못 찾는다. 실패의 *종류*를 로그에 남겨야 한다.

### 3.2 모델을 바꾸자 400 에러

**증상**: `AI_MODEL` 을 바꾸니 모든 호출이 400.

**원인**: **사고 설정 파라미터가 모델 세대마다 다르다.**

| 세대 | 파라미터 |
|---|---|
| Gemini 3+ | `thinkingLevel` (`MINIMAL`/`LOW`/`MEDIUM`/`HIGH`) |
| Gemini 2.5 계열 | `thinkingBudget` (토큰 수) |

**둘을 같이 보내면 안 된다.** 세대를 안 보고 한쪽만 고정해 두면 모델을 바꾸는 순간
조용히 잘못된 설정이 나가거나 400 이 난다.

**해결**: 모델 이름에서 세대를 파싱해 맞는 쪽만 보낸다.
```csharp
internal static object? ThinkingConfig(string model, int budget, string? level)
{
    if (budget < 0) return null;                       // 탈출구: 아예 안 보냄
    if (!string.IsNullOrWhiteSpace(level))
        return new { thinkingLevel = level.Trim().ToUpperInvariant() };
    return ModelMajorVersion(model) >= 3
        ? new { thinkingLevel = "MINIMAL" }
        : new { thinkingBudget = budget };
}
```

### 3.3 `responseSchema` 가 400

**원인**: `type` 값이 OpenAPI 의 Type enum 이라 **대문자여야 한다.**
`"object"` 는 400, `"OBJECT"` 가 맞다. 문서를 대충 읽어서 생긴 문제.

### 3.4 AI 가 조용히 꺼졌다

**증상**: 어느 순간부터 AI 가 아무 일도 안 한다. 에러도 없다.

**원인**: `.env` 의 모델명에 오타가 있었다 → 404 가 3번 쌓임 → **회로 차단기가 열렸다.**
차단기는 5분 쿨다운인데, 배치를 돌리는 동안 계속 실패해서 계속 닫히지 않았다.

**해결**: 계정에서 쓸 수 있는 모델을 먼저 확인하는 습관.
```bash
curl -s "https://generativelanguage.googleapis.com/v1beta/models" -H "x-goog-api-key: $KEY"
```
그리고 관리자 화면(`GET /api/admin/ai`)에 **회로 상태와 연속 실패 횟수를 노출**했다.

### 3.5 가사 패널이 수십 초 멈춘다

**원인**: 사다리 6단마다 검증을 걸었더니 **곡당 최대 5회 호출**이 쌓였다.
각 호출이 12초 타임아웃이라 최악의 경우 1분.

**해결**: **곡당 검증 예산 2회.** 검증은 '2차 소견'이라 위쪽 두 단에만 걸어도 오탐 대부분을 잡는다.
예산이 떨어지면 규칙 기반 판정을 그대로 믿는다 — **AI 를 끈 것과 같은 동작으로 안전하게 수렴.**

> 여기서 하나 더: **캐시 히트는 예산을 쓰면 안 된다.** 지연도 비용도 0 이니까.
> 그래서 예산 판단을 **캐시 조회 뒤**로 옮겼다.

### 3.6 커버 영상에서 올바른 원곡 가사를 거부했다 (실측 버그)

**증상**: 가사 언어 가드가 멀쩡한 일본 곡 가사를 거부한다.

**원인 추적**: 문제의 영상 제목이 이랬다.
```
DAYBREAK FRONTLINE / 텐코 시부키 cover
```
언어 가드는 "이 곡이 무슨 언어일까"를 **제목의 문자 체계**로 추정했다. 그런데 제목 **전체**를
기준으로 삼으니, 곡명은 로마자인데 **커버 가수 이름이 한글**이라 "한국어 곡"으로 판정됐다.
그래서 일본어 가사가 언어 불일치로 거부됐다.

**해결**: 기준을 **영상 제목 전체 → 곡 제목**으로 바꿨다
(후보 1순위 = 음악메타 / AI / TitleParser 가 뽑은 원곡 제목).
기준이 로마자라 언어를 못 뽑으면 **가드를 아예 적용하지 않는다** — 근거 없이 거부하는 것보다 낫다.

> **교훈**: 판정의 *입력 범위*를 잘못 잡으면, 판정 로직이 아무리 옳아도 틀린 답이 나온다.

### 3.7 가사를 바꿨는데 옛 발음이 그대로

**원인**: 발음·번역 캐시 키를 `videoId` 로 잡았다. 가사가 교체돼도 키가 같으니 옛 값이 나온다.

**해결**: 키의 입력을 **가사 본문의 SHA-256** 으로 바꿨다.

### 3.8 프롬프트를 고쳤는데 옛 답이 계속 나온다

**원인**: 캐시 키에 프롬프트 정보가 없었다. **프롬프트가 바뀌었는데 옛 답이 계속 나오면 튜닝이 불가능하다.**

**해결**: 키를 `SHA-256(작업명 + 프롬프트버전 + 입력)` 으로. 프롬프트를 고칠 때
`PromptVersion` 상수를 올리면 **옛 캐시가 자동으로 무효화된다.**

### 3.9 발음이 한 줄씩 밀렸다

**증상**: 발음이 붙긴 했는데 **한 줄씩 어긋나** 있다. 후렴 부분에서 특히 심하다.

**원인**: 모델이 빈 줄이나 간주(`♪`)를 빠뜨리거나 두 줄을 합쳤다. 그러면 그 뒤 전부가 밀린다.

**이게 가장 나쁜 실패였다.** 눈에 잘 안 띄는데(한글이 붙어 있으니 얼핏 정상으로 보인다)
실제로는 전부 틀렸다.

**해결 두 가지**:
1. 입력에 **번호를 붙여** 보낸다 — 모델이 줄 수를 세기 쉬워지고 빈 줄이 사라지는 사고가 준다.
2. **줄 수가 다르면 그 덩어리를 통째로 버린다.** 부분 수용을 하지 않는다.

```csharp
if (got.Count != src.Count)
{
    why = $"줄 수가 다르다(보낸 {src.Count}, 받은 {got.Count})";
    return false;   // 덩어리째 폐기
}
```

> **교훈**: LLM 출력은 **믿지 말고 검사해야 한다.** 특히 "개수가 맞아야 하는" 출력은 반드시 센다.

### 3.10 나무위키에서 한글만 있는 줄을 가사로 가져왔다

**원인**: 나무위키의 일본 곡 문서는 일본어 원문 아래에 **한글 발음 줄과 한국어 번역 줄**이 붙어 있다.
AI 가 원문 대신 그 줄들을 가져왔다.

**해결**: 한글만 있고 가나·한자가 없는 줄이 **절반을 넘으면** 통째로 버린다.

### 3.11 나무위키 문서를 잘못 골랐다

**원인**: 나무위키는 문서 제목이 한국어라 **제목 유사도로는 맞는 문서인지 알 수 없다.**
일본어 곡명으로 검색하면 동명이곡·번안곡·리듬게임 수록 문서가 섞여 나온다.

**해결**: 제목으로 고르지 않는다. 후보 문서를 **실제로 열어 우리 가사 줄이 그 안에 몇 % 있는지 세어**
고른다. 50% 이상이면 그 곡의 문서로 인정. 검색 결과 6개까지 열어 본다.

> 찾는 방법을 "이름이 비슷한가"에서 **"내용이 맞는가"** 로 바꾼 것이 핵심이었다.

### 3.12 빈 `layers` 가 저장되어 다시 시도할 수 없었다

**증상**: 발음 생성이 실패한 곡에 버튼을 다시 눌러도 "이미 만들어져 있습니다"가 뜬다.

**원인**: 한 줄도 못 채웠는데 빈 `layers` 를 저장했다. 다음 요청이 '이미 있다'로 막혀
**영영 다시 시도하지 못한다.**

**해결**:
```csharp
// 한 줄도 못 채웠으면 저장하지 않는다 — 빈 layers 를 넣어 두면 다음 요청이
// '이미 있다'로 막혀서 영영 다시 시도하지 못한다.
if (filled == 0) { /* 저장 생략 */ }
```

### 3.13 (인프라) 그 밖에

| 문제 | 해결 |
|---|---|
| 싱글턴이 typed `HttpClient` 를 붙잡아 핸들러가 회전하지 않음 | `IHttpClientFactory` 의 **명명 클라이언트**로, 매 호출마다 받는다 |
| 클라이언트가 떠난 뒤 부정 결과를 캐시해 **7일 부정캐시가 오염** | 쓰기 전에 `ct.ThrowIfCancellationRequested()` |
| 저사양 기기라 쿼리 하나가 실제 비용 | **키가 없으면 캐시 조회(SQLite 왕복)조차 하지 않는다** — `Configured` 를 가장 먼저 본다 |
| 네트워크 조회 동안 SQLite 연결을 붙잡아 풀이 마름 | 캐시 조회 / 네트워크 / 쓰기를 **세 개의 짧은 연결**로 분리 |
| 거절 사유를 화면에 못 띄움 | 프론트가 409 를 가로채서 — 거절을 **200 + `ok=false`** 로 싣는다 |

---

## 4. 설치·실행

> 이 저장소는 에이전트 부분만 발췌한 것이라 **단독으로 빌드되지 않는다.**
> 아래는 실제 서버에서 쓰는 설정·확인 방법이다.

### 필요한 것

- .NET 8 SDK (또는 Docker)
- Google AI API 키 — <https://aistudio.google.com/apikey>
  (`gemini-3.5-flash-lite` 는 무료 티어가 있다)

### 설정

```bash
cp .env.example .env
# .env 를 열어 GOOGLE_AI_API_KEY 를 채운다
```

```bash
GOOGLE_AI_API_KEY=your_key_here
AI_MODEL=gemini-3.5-flash-lite
AI_DAILY_CAP=1000
AI_TITLE_ENABLED=true       # 도구 A
AI_VERIFY_ENABLED=true      # 도구 B
AI_LYRICS_ENABLED=true      # 도구 C
```

### 실행 (전체 프로젝트 기준)

```bash
docker compose build && docker compose up -d
curl -I http://localhost:4156/healthz
```

### 동작 확인

```bash
# 에이전트 상태 — 모델 · 오늘 호출수 · 상한 · 회로 상태
curl http://localhost:4156/api/admin/ai

# 도구 선택 과정을 실시간으로 본다
docker compose logs -f | grep -E "AI 제목 파싱|AI 가사 거부|나무위키|lyrics "
```

### 키 없이 돌려 보기

**`GOOGLE_AI_API_KEY` 를 비운 채로 실행해도 서버는 정상 동작한다.**
AI 도구가 전부 꺼지고 규칙 기반 경로만 돈다 — 이게 설계 의도다.

---

## 5. 만들면서 배운 것

**① AI 를 어디에 *안* 쓸지 정하는 게 절반이었다.**
일본어인지 아닌지는 유니코드 범위로 확실히 아는데, 이걸 AI 에 물으면 돈도 시간도 들고
**답이 흔들리기까지 한다.** 확실히 아는 건 코드로 짜야 한다.

**② 모델에게 "모른다"고 말할 길을 열어 줬더니 정확도가 올라갔다.**
`confidence` 를 낮게 써도 된다고, `null` 을 반환해도 downstream 이 알아서 처리한다고
프롬프트에 명시하니 **억지 추측이 줄었다.** 모델은 빈칸을 싫어한다 — 빈칸을 허락해 줘야 한다.

**③ 판정을 비대칭으로 만든 게 가장 잘한 결정이었다.**
확신 있는 부정일 때만 거부하게 했다. 오탐 하나를 막으려다 멀쩡한 가사를 여러 개 잃는 건 손해다.
**"틀릴 때 어느 쪽으로 틀릴 것인가"를 정하는 게 임계값을 고르는 일보다 중요했다.**

**④ LLM 출력은 검사해야 한다.**
줄 수를 세고, 발음에 가나가 남았는지 보고, 어긋나면 통째로 버린다.
**줄이 하나 밀리는 실패는 눈에 잘 안 띄면서 가장 나쁘다.**

**⑤ 폴백을 먼저 설계하면 마음 편히 AI 를 쓸 수 있다.**
"AI 가 죽으면 어떻게 되나"를 먼저 정해 두니, 그 뒤로는 과감하게 붙일 수 있었다.
키가 없어도, 할당량이 끝나도, 응답이 깨져도 **서비스는 그냥 예전처럼 동작한다.**

---

## 6. 한계와 다음 계획

→ [`REVIEW.md`](REVIEW.md) 참고
