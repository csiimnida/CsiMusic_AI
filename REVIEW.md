# 실사용 후기

> ⚠️ **이 파일은 초안이다. 제출 전에 본인이 직접 쓴 내용으로 바꿔야 한다.**
>
> 평가 항목 4 는 "**직접 사용해보고** 실사용 후기를 작성하였다(장단점, 개선점 포함)" 다.
> 아래는 코드의 설계 의도에서 뽑아 둔 **소재 목록**이지 실제 사용 소감이 아니다.
> 실제로 며칠 써 보고, 아래에서 맞는 것만 골라 본인 경험으로 다시 쓸 것.
>
> **`TODO:` 표시가 남아 있으면 제출하지 말 것.**

---

## 1. 어떻게 쓰고 있나

`TODO:` 며칠째 / 하루 몇 곡 / 어떤 상황에서 쓰는지 (예: 자기 전에, 공부하면서, 노래방 가기 전에)

`TODO:` 보관함 규모 — 곡 수, 그중 일본 곡 비율

---

## 2. 좋았던 점

> 아래에서 **실제로 겪은 것만** 고르고, 구체적인 곡 이름이나 상황을 붙일 것.
> 구체적인 사례 하나가 일반적인 칭찬 열 줄보다 설득력 있다.

- **커버 영상에서 가사가 나온다.**
  규칙 기반으로는 원곡 아티스트를 못 뽑아 실패하던 케이스. VTuber 커버나 `歌ってみた` 영상이
  많은 보관함이라 체감 차이가 가장 컸다.
  `TODO:` 실제로 이걸로 가사가 나온 곡 하나를 예로 들 것

- **엉뚱한 가사가 뜨는 일이 줄었다.**
  예전엔 제목만 비슷한 다른 곡 가사가 뜨면 그냥 참고 봤는데, 이제 검증이 걸러내고 다음 소스로 내려간다.
  `TODO:` 로그에서 `AI 가사 거부:` 를 실제로 확인했는지, 어떤 사유였는지

- **일본 노래를 따라 부를 수 있게 됐다.**
  이게 사실 이 기능을 만든 진짜 이유다. 장음을 늘려 쓰기로 한 결정이 박자를 맞추는 데 실제로 도움이 됐다.
  `TODO:` 줄여 쓴 표기와 비교했을 때 어땠는지

- **AI 가 없어도 서버가 멀쩡하다.**
  `TODO:` 할당량이 끝났거나 키를 뺀 상태로 써 본 적이 있는지 — 있으면 그 경험을 쓸 것

- **돈이 거의 안 든다.**
  캐시 덕분에 같은 곡을 다시 틀어도 호출이 0 이다.
  `TODO:` `GET /api/admin/ai` 의 `callsToday` 를 며칠 관찰한 실제 숫자

---

## 3. 아쉬운 점

> **솔직하게 쓸수록 좋다.** 단점을 못 찾았다고 쓰면 오히려 "안 써 봤다"로 읽힌다.

- **한자 읽기를 가끔 틀린다.** 노래에서만 쓰는 특수한 읽기는 모델도 모른다.
  `TODO:` 실제로 틀린 사례

- **번역이 줄 단위로 끊겨 어색할 때가 있다.**
  한 문장이 두 줄에 걸치면 각 줄이 반쪽짜리가 된다. "줄 경계를 넘지 말라"는 프롬프트 규칙 때문에
  생기는 **의도된 트레이드오프**지만, 읽을 때는 어색하다.

- **일본어만 된다.** 영어·중국어 곡은 대상이 아니다.

- **버튼을 눌러야 만들어진다.** 자동으로 안 도는 건 비용 때문인데, 쓰는 입장에선 한 단계 귀찮다.

- **검증이 가끔 멀쩡한 가사를 거부한다.**
  거부되면 7일간 부정캐시라 바로 되돌릴 수 없다.
  `TODO:` 실제로 오거부를 겪었는지 — 겪었으면 그 곡과 사유

- **나무위키 의존이 불안하다.** 문서 구조가 바뀌면 파싱이 깨진다.

---

## 4. 고치고 싶은 것

- **거부 로그를 화면에서 볼 수 있게.**
  지금은 `docker compose logs` 를 봐야 한다. 관리자 화면에 "최근 AI 거부 목록 + 되돌리기 버튼"이
  있으면 오거부를 바로 수습할 수 있다.

- **번역 문맥을 늘린다.**
  지금은 청크 경계에 2줄만 겹친다. 곡 전체를 한 번 읽히고 줄별로 번역하면 문장이 걸친 줄도
  자연스러워질 것 같다.

- **도구 선택 과정을 사용자에게 보여 준다.**
  `GET /api/lyrics/{vid}/trace` 로 "어떤 소스를 왜 건너뛰었는지"를 보여 주면,
  가사가 이상할 때 원인을 바로 알 수 있다.

`TODO:` 실제로 쓰면서 불편했던 것 1~2개를 더 추가할 것 — 이게 가장 설득력 있다

---

## 5. 만들면서 느낀 것

> 아래 중 **공감되는 것만** 골라 본인 말로 다시 쓸 것.

- **AI 를 어디에 안 쓸지 정하는 게 절반이었다.**
  확실히 아는 건 코드로 짜야 한다. AI 에 물으면 돈도 시간도 들고 답이 흔들리기까지 한다.

- **모델에게 "모른다"고 말할 길을 열어 줬더니 정확도가 올라갔다.**
  모델은 빈칸을 싫어한다 — 빈칸을 허락해 줘야 한다.

- **"틀릴 때 어느 쪽으로 틀릴 것인가"를 정하는 게 임계값을 고르는 일보다 중요했다.**

- **LLM 출력은 믿지 말고 검사해야 한다.**
  줄이 하나 밀리는 실패는 눈에 잘 안 띄면서 가장 나쁘다.

- **폴백을 먼저 설계하면 마음 편히 AI 를 쓸 수 있다.**
